import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.infintro.samartlampcontroller',
  appName: 'SLC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
